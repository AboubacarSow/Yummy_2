@model List<Yummy_2.Models.SocialMedia>


<div class="col-lg-3 col-md-6">
    <h4>Bize Takip Ediniz</h4>
    <div class="social-links d-flex">
        @foreach(var item in Model)
            {
                <a href="@item.Url" class="@item.Title"><i class="@item.Icon"></i></a>
            }
       
    </div>
</div>

